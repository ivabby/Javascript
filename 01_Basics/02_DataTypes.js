"use strict"; // treat all JS code as newer version

// alert("Hello"); 
// It will not work in JS directly. It will work in browser side

let name = "Sam" // String
let age = 20 // Number
let isLoggedIn = false // Boolean
let state;

// null is object/ standalone value
console.table([typeof name,typeof age,typeof isLoggedIn,typeof null,typeof state]);
//  undefined means value not defined

//  symbol used to find uniqueness